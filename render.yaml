services:
  - type: web
    name: telegram-night-link-cleaner
    runtime: python
    plan: free
    autoDeploy: false
    envVars:
      - key: BOT_TOKEN
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -b 0.0.0.0:$PORT bot:app
